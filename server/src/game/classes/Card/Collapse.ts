import { Card } from "./Card";
//Classe Collapse: les cartes éboulement pour détruire une partie du chemin crée par les joueurs
export class Collapse extends Card {
  constructor(cardId: number) {
    super(cardId);
  }
}
